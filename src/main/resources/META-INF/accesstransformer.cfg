# Minecraft only lets the third-person camera sit a fixed distance behind the player's eyes, and
# keeps the position setter to itself. An aircraft needs the camera further back and higher up than
# any mob does, and a tank needs it further back than its own length, so open the setter up;
# ChaseCamera and the two vehicle camera handlers are the only callers.
public net.minecraft.client.Camera setPosition(Lnet/minecraft/world/phys/Vec3;)V
